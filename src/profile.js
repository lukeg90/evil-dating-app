import React from "react";

export default function Profile(props) {
    return (
        <React.Fragment>
            {props.profilePic}
            {/* <BioEditor /> */}
        </React.Fragment>
    );
}
